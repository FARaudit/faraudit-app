---
name: design-retirement-redesign-review
description: Retirement-redesign panel (2026-07-22) — falsification of the frozen-ledger datum STANDS but proves too much; card #682's REJECT survives on the substrate-independent FAIL-2; grade D
metadata:
  type: project
---

Panel-on-design, `ceo/RETIREMENT-REDESIGN-DESIGN.md`. My ruling: `ceo/PANEL-REDESIGN-REDTEAM.md`. Grade **D**,
NOT survivable as a build order as ordered.

**Why:** both other seats falsified the arc's only measured false-BID (`70B01C 999e909b`: frozen `coverageV2`
12 entries → 0 under today's classifier). I reproduced it and failed to break it three ways — including the one
posture ex-KO never ran (`AUDIT_BOILERPLATE_BAR_SIGNAL_GUARD=true`, which *adds* escalation; still 0).

**Why:** two seats agreeing is when a panel is most likely collectively wrong — and here they were, twice:
1. The frozen-ledger doctrine **proves too much**. It voids the flag-OFF 28/28 baseline by the identical
   argument ⇒ the arc's only ratified arm gate is void in both directions. Neither seat priced that.
2. They concluded "the veto has no true positives" from a bank that is structurally blind to the veto's
   protected class (§{B,C,D,E,F,H} = 0 of 323 bucket entries). Absence of evidence ≠ evidence of absence — the
   same epistemic move as trusting a frozen ledger.

**The move that survives everything:** separate ledger-derived evidence from **synthetic-input, live-code**
evidence. Card #682's FAIL-2 (retirement silently co-retires the ARMED `AUDIT_COVERED_DIRECT_BAR_FLOOR`) and the
A1+ register probe (4/10 PROTECTED, all by the veto) ran through real `completenessOf` over a constructed
context and never touched a banked record ⇒ substrate-independent ⇒ the REJECT holds without the false-BID.

**Why:** always re-check the coordinator's own "confirmed by me" facts. The brief's D3 violation
(`CONFIG_FLAGS` missing `AUDIT_SELF_CLEARABLE_PACKAGE`) was STALE — `CONFIG_FLAGS` no longer exists; the stamp
now enumerates every `AUDIT_*` var by construction and the flag IS printed. Carried-forward evidence committed
by the panel's coordination layer while ruling on carried-forward evidence.

**How to apply:** when a fixture-based finding is falsified, immediately ask (a) what ELSE shares that substrate
(usually the baseline that made the finding look decisive), and (b) which findings are synthetic/live-code and
therefore immune. Grade the doctrine, not just the datum: a parity rule that retires the only merge instrument
must ship with its replacement named. See [[gauntlet_hardbar_r1]] (bank-blindness) and
[[design_step4_veto_retirement_review]] (the prior round).

Flag ruling: KEEP `AUDIT_RETIRE_VERBATIM_VETO` flag-OFF (it sits in the INSTRUMENT position, not a guard seat —
the #677 placebo precedent does not transfer), + header stamp + a wiring-suite assertion pinning the
co-retirement. Banner: `disqualifierUncovered[0]` is document order — verified, a bid guarantee sat at index 39
unquoted while a debriefing sentence was shown; ranking is **cap-invariant, reason-variant**, not "verdict-inert
by construction."
</content>
