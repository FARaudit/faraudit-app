---
name: design-root2-coverage-exists-review
description: ROOT-2 EXISTS-denominator design review (card #648 arc) — grade C; 3 P0s incl. denominator-provenance self-defeat + near-dup false-INCOMPLETE + unsound chars/byte floor; budget-drop-must-gate premise REFUTED by C-1 doctrine
metadata:
  type: project
---

Panel-on-design review of `ceo/ROOT2-COVERAGE-EXISTS-DESIGN.md` (2026-07-21). Verdict: DESIGN HAS HOLES, grade C.

**DRAFT-2 delta (2026-07-21, second review):** draft 2 adopted `max(manifest.length, resourceLinks.length)` (fixes the count leg of P0#1 for the one-endpoint-degraded shape) and REVERSED the budget-drop premise (budget drops now correctly stay INCOMPLETE via overflow — aligns with C-1). STILL LIVE: expectedBytes remains v3-self-referential + `sizeBytes:null` collapses the floor (sam-attachments.ts:384); both-degraded/v2-lag (resourceLinks `[]` — watcher-tick exists because v2 lags) + the `expectedDocs==1` guard reproduce #648; near-dup double-posts false-INCOMPLETE via raw-count max; chars/byte density floors unsound both directions (the design's own 0.02 PDF ratio barely reaches the proven 3KB stub); shadow pole (audit-decide.ts:3110/3148) retired documentsComplete → fix evaporates at Phase-2 flip; gate ladder verified: GATE_V2 + noticeBodyBar fire BEFORE documentsComplete (1b, decide:3255). NOTE: audit-decide.ts defeats BSD grep (encoding/long lines) — use awk or LC_ALL=C.

**Ground truth established (reusable):**
- Completeness truth = `agenticManifestComplete` (audit-executor-v3.ts:80-88: `files_ingested >= files_total && !overflow && !hasBindingContentLoss`) → `VerdictInputs.documentsComplete` (audit-orchestrator.ts:2757) → hard INCOMPLETE at audit-decide.ts:3255. `files_total` comes from the v3 RESOURCES manifest; `resourceLinks` from the v2 OPPORTUNITY record — different endpoints, different failure domains. Seq-4 blip = v3 degraded to 1 entry, route held 6 v2 links, never cross-checked.
- Budget drops ALREADY force INCOMPLETE today (overflow leg + executor:76/398/645-649). The "budget drop is honest ⇒ must not gate" framing contradicts ratified C-1 doctrine (audit-findings.ts:197-203: unread binding doc could carry OR waive a bar). Budget/blip distinction lives in per-file reason strings + ROOT-1 retry policy, never the pole.
- Shadow pole (`deriveShadowVerdict`, audit-decide.ts:3110-3152) RETIRED documentsComplete and reads GATE_V2 + a DIFFERENT manifestComplete signal — any completeness fix threaded only into documentsComplete evaporates at the Phase-2 flip (instance of [[project_routing_v2_predicate_reader_divergence]]).

**P0s found:** (1) expectedDocs/expectedBytes sourced from the v3 manifest = same degraded endpoint the fix audits → seq-4 reproduces under the fix's name; expectedBytes has NO independent source at all. (2) resourceLinks counts SAM double-posts that reconcileNearDuplicates (sam-attachments.ts:793-806, 1198-1200) deliberately excludes → chronic false-INCOMPLETE. (3) chars/byte per-format floor unsound both directions (image-heavy docx <0.001; 1% floor passes a 4KB stub's cover paragraph); the real stub invariant is delivered `buf.length` vs manifest `sizeBytes` — format-independent, already in the meta, absent from the design. Floor also lacked isBindingDoc carve-out (would re-open Brain #346 PPQ / #406 roster false-INCOMPLETEs).

**Other holds:** gate ORDER unspecified — noticeBodyBarUngrounded NHR + GATE_V2 NHR fire BEFORE documentsComplete, so the design's own "stub+bar ⇒ INCOMPLETE never NHR" gold specimen fails unless the EXISTS gate is ordered above them. New VerdictInputs = dual completeness axis violating C-1 single-truth; right seam = fix the denominator inside assembly/agenticManifestComplete. expectedDocs==1 guard blesses the stale-v2/both-degraded shape (watcher-tick exists because resourceLinks lag reality). Cost-prescreen arms only on manifestComplete=true (executor:436) → newly-incomplete blip packages bypass the cost gate yet still spend the panel.

**How to apply:** on any completeness/denominator design — (a) demand PROVENANCE of every denominator (which endpoint, which failure domain; a check sourced from the thing it audits is void), (b) prefer byte-count-vs-promised-size over char-density heuristics for stub detection, (c) trace the signal to the EFFECTIVE verdict pole under all flag combos incl. the shadow, (d) verdict-gate ordering is load-bearing — a spec that names a target pole must name its position in the hard-gate ladder.
