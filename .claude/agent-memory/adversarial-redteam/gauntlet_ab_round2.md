---
name: gauntlet-ab-round2
description: SECOND independent gauntlet on the FIXED Invariants A+B (post "20->0" claim) — all 9 prior classes CLOSED on exact re-run, but 9 NEW break classes (6 critical NHR->BID flips proven e2e); grade F
metadata:
  type: project
---

# Gauntlet round 2 — Invariants A+B post-fix (feat/instrument-governance-ab) · 2026-07-21

Fixtures: `/tmp/gauntlet-ab-round2.ts` + `/tmp/gauntlet-ab-round2b.ts` (verdict-flip compositions); Part 1 re-ran the EXACT round-1 hostiles (`/tmp/gauntlet-ab-hostile.ts` + `-e2e-cap.ts`). All real exports; $0; no prod code touched.

## PART 1 — all 9 prior classes CLOSED (49/49 HOLDS + 3/3 cap-kept)
Pool 5/5 refuse · consequence-riders 5/5 refuse · S3 blank+"Insert N/A" escalates · S5 amendment-in-requirement joins family · S7 "(C3PAO)/N/A: [ ]" + box-drawing escalate · S4 requirement-reference not picked governing · 7021 src-scan fires · 7012-flowdown cuiSignal→BWC · definitional bar disposes "dropped" e2e · flag="1" split-brain closed (all 3 sites isEnvOn).

## PART 2 — 9 NEW classes (every fix reopened its seam one axis deeper, + the fixes ADDED two brand-new false-BID mechanisms)

**CRITICAL — verdict flip NHR→BID proven through deriveVerdict (round2b):**
1. **N1 instruction-N/A verb blocklist.** NA_INSTRUCTION_RE = verbs insert|enter|mark|select|check|write|type + "N/A if". "indicate N/A when / state N/A where / annotate N/A for" leak → BLANK cell dormant-izes. S3 reopened.
2. **N2 XOR tell is type-token vocab.** ASSESSMENT_TYPE_RE=self|c3pao|dibcac; one OCR glyph (C3PA0, Se1f, D1BCAC) kills the type half AND the level half → not-XOR → column-bleed N/A dormant-izes. S7 reopened (the R3b input one glyph off).
3. **N3 range-cite governing pick.** findingCitationCarriesSubpart = citation CONTAINS "(b)(1)"; a summary finding cited "252.204-7025(b)(1)-(d)" whose excerpt is the CONDITIONAL "if (b)(1) is marked N/A…" is picked governing; parseFillInValue finds the conditional N/A → deps dormant with NO election. S4 reopened.
4. **N4 heterogeneity needs the numeric root.** Amendment "Section I, paragraph (b)(1) of the CMMC assessment clause is revised… Level 2 (C3PAO)" (no 252.204-7025 in cite/requirement) never joins byRoot → invisible → base N/A dormant-izes under a live C3PAO amendment. S5 reopened.
5. **N5 app-2 re-type ORs requirement/excerpt.** classifyDefinitionalSpan(requirement) || (excerpt): a real bidder_cannot_move QPL bar whose EXCERPT mis-grounded to the neighboring (a)-definition → kind→boilerplate → dropped. NEW mechanism introduced BY fix F9 (ignores contradicting sibling field + requiredAttribute marker).
6. **N6 unquoted definiendum branch = any Capitalized-NP + "means".** "Failure to attend the site visit means elimination from the competition" → definitional:true (elimination/removal/cannot-advance are in NO refusal RE) → gateV2 CAP RELEASED + app-2 drops the typed bar. DEFINITIONAL_SHAPE_RE `[A-Z][\w .()/-]*\s+means` matches "means" as a plain consequence VERB.
7. **N7 pool gate is still adjective×noun VOCAB.** Nouns facility/technician/laboratory unlisted; the "positive inert-definiens gate" is 3 NEGATIVE screens (BAR_SIGNAL/RESTRICTION/POOL) — "Eligible repair facility means a facility within fifty miles of Naval Station Norfolk" releases → cap released + bar dropped. Doctrine violation persists: the RELEASE rests on blocklists failing to match.

**Moderate:**
8. **N9 cuiSignal residual (prior class 8).** Verb-less affirmative artifact ("A DD 254 is attached") + distant negation → false → BID not BWC. Also "DD Form 254" NEVER matches CUI_TOKEN_RE (`dd[\s-]?254` has no room for "Form").
9. **N10 companion scan misses U+2212.** normClauseCiteBoundary normalizes only U+2010–U+2015; "252.204−7021" (math minus, real PDF substitution) → companion missed → wrongful dormancy with 7021 present. En-dash control HOLDS.

**Over-escalation (safe, but the feature under-delivers its own motivating case):**
- N8a: a realistic 7021-style definition with "certification" in the definiens → refused → the N0016-class false-NHR RETURNS on real defs.
- N8b: canonical checkbox-menu fill-in (all levels printed + N/A elected) → onSchema trips on the MENU → Invariant B inert on the most common real format.
- N8c: real N/A + template instruction in one excerpt → refused (excerpt-wide instruction veto).

## HOLDS (round 2)
XOR no-misfire on valid "Level 2 (C3PAO)" · U+FFFD short-span garble escalates · bare "7021" (help-desk ext) no false-trip · "CMMC Level: N/A" + table-cell parse correctly dormant · instruction-context blank (N12c) escalates · flag-OFF byte-identity on every new hostile.

## Verdict
GRADE **F** — "20→0" is true ONLY against round-1's exact fixtures. Every guard was reconstructed one axis wider (vocab lists), and fixes F9 (app-2 OR-fields) + the unquoted shape branch are themselves NEW critical false-BID vectors. Classic [[feedback_reconstruction_treadmill_pivot_recognizer]] staircase. Pivot demanded: dormancy only when the parsed VALUE is EXACTLY an N/A token (whole-value equality after artifact strip, never substring); definitional release only on the QUOTED-definiendum shape (kill the bare capitalized branch); definiendum pool-class by adjective-SHAPE × ANY noun; app-2 drop requires BOTH fields definitional AND no requiredAttribute; governing citation must be EXACTLY the governing subpart (no ranges).
