---
name: sweep-root1-no-silent-degrade
description: ROOT-1 (Brain #648) no-silent-degrade fix review — FIX HAS HOLES grade F; guard is dead code on the production worker path; recorded seq-4 trace shows degraded-200 1-entry manifest SUCCEEDED on the worker (not assembly-null on the route); card-648 autopsy inference unsound (compliance_json.ingestion never persisted by v3)
metadata:
  type: project
---

Review of the ROOT-1 fix (retry + NO-SILENT-DEGRADE guard, 2026-07-21, working tree). Verdict: FIX HAS HOLES.

**Decisive evidence chain (reusable):**
- Card 645 telemetry line `[timing] prepanel:sam-retrieval(assembleSamDocumentSet) 1134ms · 1/1 docs` exists ONLY at `agents/audit-worker/worker.ts:557` ⇒ seq-4 8ca6d7d4 ran on the WORKER and assembly SUCCEEDED with a 1-entry manifest (`docs_complete=true (1/1)` consistent with ingestion={1,1}). Card 648's counter-story ("route single-doc fallback, assembly null") rested on "stored compliance_json.ingestion = NONE" — but NOTHING in the v3 path ever persists that key (ingestion surfaces only as `payload.documents`, executor-v3:618-644), so its absence proves nothing. Autopsy inference UNSOUND; matches my ROOT-2 memory ("v3 degraded to 1 entry, route held 6 v2 links").
- Production route is AUDIT_ASYNC_ENQUEUE=true → route returns 202 at route.ts:320-322 BEFORE the guarded block (437) — the route guard is dead code on the paid path. Worker buildInput (worker.ts:542-616) has NO guard; refetch route (refetch/route.ts:186) has NO guard. Only watcher + non-prod sync are guarded.
- The retry in fetchAttachmentManifest fires only on !res.ok/throw. A degraded 200 (partial attachment list) is returned as truth — the recorded failure mode passes untouched. Two more silent-shrink-on-200 vectors: entries missing resourceId/name are silently filtered; only `opportunityAttachmentList[0]` is read.
- The null-assembly fallback was ALREADY verdict-capped: `agenticManifestComplete(null, _, isSamSol=true)` = false (executor-v3:78,87) → INCOMPLETE. So the guard protects a path that could not report COMPLETE anyway; the path that CAN report COMPLETE (degraded-200 → ingestion N/N on a shrunken N) is unguarded.
- Budget-cap leg is SOUND: skipped>0 → overflow (sam-attachments.ts:1234) → INCOMPLETE; truncated binding docs → bindingContentLoss (executor-v3:100). Attachments-without-primary impossible by construction (sam-attachments.ts:1216-17).

**Round-2 re-verify (2026-07-21, later working tree):** all round-1 holes still open (worker.ts:569-616 unguarded, refetch:186 unguarded, route guard post-202). TWO NEW: (1) guard OVER-FIRES on assembled-no-primary (manifest READ ok, zero docs ingestible e.g. all-oversize — sam-attachments:1216 primary null iff downloaded empty): route.ts:432-437 sets ingestion then guard blocks the legacy Files-API oversize read that would have produced an honest-INCOMPLETE audit WITH content; on watcher this hits `return {ok:false}` (watcher-tick:179) = NO audit at all; reason string "assembly failed" is false. Guard must key on `assembled === null`, not !pdfBase64. (2) retry × FETCH_TIMEOUT_MS(30s) worst-case ~91s inside resolve/route.ts maxDuration=30 → function-killed where pre-fix returned no_documents in ≤30s; retry budget must be caller-parameterized. Mitigation confirmed: single-doc fallback on SAM sols is verdict-capped INCOMPLETE by agenticManifestComplete(null,_,isSamSol) (executor-v3:78-87,163) — so unguarded paths degrade-with-honest-flag, never false-COMPLETE; the ONLY false-COMPLETE vector remains degraded-200 (files_total=N' shrunken, N'/N' complete).

**How to apply:** (a) before grading any "fix for incident X", re-derive X's mechanism from the RAW telemetry, not the autopsy card — locate which file emits each logged line to pin the surface; (b) absence of a field proves nothing until you confirm the field is ever written on that path; (c) retry-on-failure never covers degraded-success — ask what a 200 can silently omit; (d) any guard added to route.ts must be checked against the AUDIT_ASYNC_ENQUEUE early-return (route code after line ~320 does not run in prod). Sibling: [[design-root2-coverage-exists-review]] (the EXISTS-denominator cross-check is the real closure for this class).
