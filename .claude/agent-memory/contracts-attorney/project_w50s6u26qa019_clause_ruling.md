---
name: w50s6u26qa019-clause-ruling
description: "W50S6U26QA019 (SDVOSB landscaping) clause-lens ruling — F on 2 AUTO-F: hallucinated Section-E absence (3rd run) + 52.222-42 read as a wage floor against its own excerpt; clause FABRICATION still ZERO (3 runs); the UNVERIFIED-ABSENCE hedge reached findings but NOT the coverage/reason string"
metadata:
  type: project
---

Audit `eab43ada-2baf-49e2-b224-a968df7864f3` · notice `1e3e02dbe95e4561a522d902824060d5` · INCOMPLETE/NO VERDICT, 70 findings. **Grade F, 2 AUTO-F.**

**Why:** ruling banks three things that generalize past this solicitation — one fix that is working, one fix that only got half-wired, and a new failure class.

**How to apply:**

1. **Clause fabrication is ZERO for the 3rd consecutive run.** Set-difference of all 24 cited clause numbers against 73 in the real source: no invented, no mis-numbered, no mis-sectioned (one exception below). This is held-constant on the lens and is now a believable engine property — say so out loud, don't re-litigate it. See [[project_w9123826qa032_hallucinated_absence]].
2. **The `UNVERIFIED ABSENCE` hedge is wired into finding `requirement` strings but NOT into `v3.coverage` / `v3.reason`.** Findings #1 and #62 hedged correctly; the top-line `"binding section(s) not located: E"` did not — and Section E existed at page 26. **The hedge must follow the claim into the coverage/reason carrier, or the fix is cosmetic.** Third hallucinated-absence in a row.
3. **NEW CLASS — `read-in-full` mislabeled as `could not be read`.** `documents.unanalyzed[]` says all 7 docs were "read in full, but no finding was grounded in it"; the customer sentence says they "could not be confirmed read in full (unfetched, scanned/no-text, or truncated)." **The engine knows the difference and the render throws it away.** Inverse of [[feedback_coverage_measures_ingestion_not_analysis]] — that one over-counted read as analyzed; this one under-reports analyzed-failure as read-failure.
4. **`amendment_disclosure` exists in the persisted row and renders NOWHERE.** Zero grep hits for "Superseded terms" in the export. A supersession warning generated then suppressed by the render path — check the render, never the row. See [[feedback_verify_the_rendered_output_not_the_engine]].
5. **A clause that disclaims itself is a trap the grounder cannot see.** FAR 52.222-42's own text — displayed as the finding's excerpt, directly beneath the claim — reads "This Statement is for Information Only: It is not a Wage Determination." The finding turned its $29.99 into a binding price floor. Real WD 2015-5613 R32: Gardener $23.05, Grounds Maintenance Laborer $19.67. Rule 64 passed it because the *excerpt* was verbatim. Textbook [[feedback_grounding_checks_excerpt_not_claim]]. **Clauses whose operative content is a disclaimer: 52.222-42, 52.252-1/-2/-6, 52.252-5.** The real floor is RFO **52.222-41(c)(1)**: *"Each service employee … by the Contractor **or any subcontractor** must be paid not less than the minimum monetary wages … as specified in **any wage determination attached to this contract**."* — so on any SCA job the WD attachment is the floor, it reaches the sub directly, and **52.222-41(c)(2)(i) conformance** is the unpriced risk when the PWS scope (here: palm removal, Desert Willow install, establishment watering) doesn't map onto a listed class.
5b. **THE GAP LIST ITSELF LEAKS — `analyzed` is computed, not derived from provenance.** `documents.analyzed = 3`, but `finding_provenance` names only **2** sources carrying any grounded finding (the solicitation + SAM Notice Body). Of **10** read files, **9** produced zero findings; the customer gap list names **7**. Silently omitted: `Site_Visit_Sign-in…pdf` and — worse — `Attachment_0005_Past_Performance_Reference_Fillable_Form.docx`, the submission vehicle for one of §M's two evaluation factors. **Probe:** `set(read_modes) − set(finding_provenance.doc)` must equal `documents.unanalyzed`; here it is 9 vs 7. An honest-fail gate whose own gap list under-reports is a placebo in the [[feedback_placebo_family_inert_equals_passing]] family. Found only on the 3rd discipline pass.

6. **SDVOSB set-aside reconciliation, verified live and reusable.** DoD class deviation **2026-O0037** (eff. 2026-02-01) makes the **RFO FAR Part 19** text controlling over 48 CFR ch.1 — so verify prescriptions there, not only the codified FAR. RFO **19.104-3(c)(1)** requires **52.219-14** on any 19.106 (SDVOSB) set-aside **regardless of dollar value**; RFO **19.104-3(a)** limits **52.219-6** to *total* small business set-asides. This solicitation has 52.219-6 in §I and no 52.219-14 anywhere. **I tried to break this and failed — read the DEVIATED bodies on the RFO Part 52 page, don't stop at the prescription.** RFO **52.219-6(c)(1)**: *"Offers are solicited only from **small business concerns**. Offers received from concerns that are not small business concerns shall be considered **nonresponsive and will be rejected**."* Sitting beside 52.219-27, that is an **operative eligibility contradiction** about who may compete — not clause housekeeping — and it is patent, so waived at closing under 4 CFR 21.2(a)(1). RFO **52.219-14(e)(1)** carries the 50% services limit and binds *"By submission of an offer and execution of a contract"*; 13 CFR 125.6(a)(1) binds regardless. RFO Part 52 also confirms 52.219-14 is **live, not [Reserved]**, under the overhaul. **The audit surfaced neither, and never mentioned 52.244-6 — the commercial-subcontract flowdown clause, i.e. the one clause a sub-facing product cannot omit.**
7. **acquisition.gov CloudFront returns 504 under any burst** — retry 3-5x with 8s sleeps, it clears. eCFR versioner API (`/api/versioner/v1/full/<date>/title-48.xml?...`) works and is the faster cross-check; its most recent issue date lags today by days, so query the lag date not today.
