---
name: hardbar-pivot-panel-ruling
description: "Ex-KO seat ruling on hard-bar floor PIVOT design (card #673, 2026-07-22): retire prose part A, keep structured part B; H1 DIES (shared fire/suppression vocab), H2 survives verb-limited"
metadata:
  type: project
---

Panel-on-design ruling (2026-07-22, `ceo/PANEL-PIVOT-EXKO.md`): INVARIANT AMEND — offer-time possession is the right
over-fire guard but models only the MINORITY register (R7 clean prose frame); genuine KO bars live in R1 structured
artifacts (SAM set-aside code/SF1449 blk 10/52.219-x), R2 enumerated lists, R3 submit-proof-§L, R4
consequence-of-absence gates (EVAL_PREFERENCE kills "will not be evaluated" — a gate, not a preference), R5
classified-annex access, R6 DD-254 form fields. **H1 DIES**: (1) fire-path term allowlists also gate SUPPRESSION →
missing member = false NHR over a handled bar (verified: "DD Form 254"/"cleared at the SECRET level" miss
TERM_CLEARANCE → suppression bypass; BRK-12 was this live); (2) "safe while veto stands" is void — ruling 1 arms the
floor exactly when the veto retires. **H2 SURVIVES AMENDED**: possession-STATE verbs only (hold/possess, never
maintain/obtain), head-NP-enforced. **Q3**: prose floor part A = retire (4/40 fires, all set-aside, zero deltas =
§5 near-inertness pre-trigger); part B set-aside (structured-keyed, BWC-cap) = keep; vehicle_holder = phantom on
the SAM front door (FAR 5.202 exempts 16.5 orders from synopsis — live-verified); placebo-floor-at-veto-retirement
named the worst outcome → re-scope PANEL RULING 1 to demand the obligation LEDGER, not this floor's flag.

**Why:** three rounds of evidence (R1 14-break close → ULTRA D one axis over → 4/40 inert) + register analysis.
**How to apply:** in any future hard-bar/veto-retirement review, check the moves-1+2 gate does NOT treat an R7-only
prose detector as the certified false-BID backstop; check suppression vocab is decoupled from fire vocab.
Related: [[project_verdict_arc_temporal_ruling]] [[project_ultra_b2_temporal_f1_datetime_parse]]
