# SP3300-26-Q-0165 — Card 191 PAID RUN RECORD (Rule-20)

**First real paid audit run.** CEO-greenlit 2026-07-01. One paid call, no retry (Rule-68). Runner: `scripts/audit-ai/card191-paid-run.ts`.

## Invocation
- Live agentic pipeline (`auditPackage`) over `SP3300-26-Q-0165-FULL-SOURCE.txt` (37,643 bytes), `bidderProfile=null` (null-world), `naics=null`, `setAside=null`.
- Run-env flags (ON for this run only; prod stayed OFF on both Vercel `faraudit-app` + Railway `audit-worker`): `AUDIT_SETASIDE_OVERTYPE_GUARD=true AUDIT_HONESTFAIL_NO_CHARGE=true`.
- Wall-clock ~120s. Models: Haiku read → Sonnet lens/base-skeptic → Opus escalation on contested findings → deterministic `deriveVerdict`.

## Result vs frozen expectation
| Assert (card 191 §2) | Expected | Live result | |
|---|---|---|---|
| a) verdict | `NEEDS_HUMAN_REVIEW` | **`INCOMPLETE`** | ❌ |
| b) eligible | `!== false` | **`false`** | ❌ |
| c) honest-fail → quota NOT decremented | `billable === false` | **`billable === false`** | ✅ |

`coverageComplete=false` · `conflict=false` · 27 findings · sections read B,C,H,I,K,L,M · reason: *"Coverage not complete — not all binding content was read and grounded."*

## Root cause (diagnosed $0)
`SP3300-26-Q-0165` is a **COMMERCIAL FAR Part-12 combined synopsis/solicitation** (`52.212-1/-2/-3/-4/-5`, `52.219-30` WOSB) with **no §A–M UCF headers**. The completeness gate keys on Part-15 UCF binding sections (B/C/H/I/L/M); on a Part-12 doc those obligations don't ground → `coverageComplete=false` → **`INCOMPLETE` short-circuits `deriveVerdict` BEFORE the set-aside→NHR disposition (step-5b) runs.** The disposition code (`applyAwardBasisOvertypeGuard`→"nhr") is correct — it was **unreachable**. Known Part-15-assumption gap (commercial section detection).

## What the run proved
1. **Honest-fail no-charge works live** — `INCOMPLETE ∈ HONEST_FAIL_VERDICTS` → `billable(true,true)=false` → quota NOT decremented, customer not charged. ✅
2. **Zero-contract-loss doctrine HELD** — engine failed SAFE to an honest-fail; no false committal (no false NO_BID / INELIGIBLE) shipped. ✅
3. NHR disposition remains **unproven-live** (needs a true UCF Part-15 negative anchor — Brain card 193 recon).

## Cost close-out (REAL)
- Console balance BEFORE = **$58.93** credits · AFTER = **$57.91** credits → **$1.02 consumed** (Console "Total Token Cost" = **$1.05**).
- vs estimate $0.41–0.93 → **1.13× top-of-range** (extra section reads + Opus skeptic escalation), **< 2× (no gap-explanation trigger)**, well under the **$2 hard cap**. PENDING→REAL.

## After-state
Prod flags UNSET both surfaces · HEAD `e61d304` at run time · nothing from the run committed to the engine · anchor reclassified **deriveVerdict-unit scope** in `gold-set-registry.json` (card 193). Frozen artifacts untouched.
