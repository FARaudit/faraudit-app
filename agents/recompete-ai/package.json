{
  "name": "recompete-ai",
  "version": "0.1.0",
  "description": "FARaudit Recompete AI — Railway daily worker. Scans audits.outcome=won, estimates contract duration, flags expiring within 180d, Telegram-alerts CEO once per audit (dedup via audits.recompete_alerted_at). Alert-only since 2026-07-29.",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "dry-run": "DRY_RUN=true tsx index.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.74.0",
    "dotenv": "^17.4.2",
    "tsx": "^4.21.0",
    "ws": "^8.18.0"
  }
}
