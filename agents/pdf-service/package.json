{
  "name": "pdf-service",
  "version": "0.1.0",
  "description": "FARaudit PDF Service — Railway-hosted HTML→PDF renderer. Vercel proxies POST /pdf with rendered audit HTML and gets back a PDF byte stream. Splits the heavy headless-Chromium dependency off the Vercel lambda where serverExternalPackages + the file-tracer don't reliably ship @sparticuz/chromium's brotli binary.",
  "main": "index.ts",
  "type": "module",
  "scripts": {
    "start": "tsx index.ts",
    "dev": "tsx watch index.ts"
  },
  "engines": {
    "node": ">=22"
  },
  "dependencies": {
    "@sparticuz/chromium": "^149.0.0",
    "express": "^4.21.2",
    "puppeteer-core": "^25.1.0",
    "tsx": "^4.21.0"
  },
  "devDependencies": {
    "@types/express": "^4.17.21",
    "@types/node": "^22.10.5",
    "typescript": "^5.7.3"
  }
}
